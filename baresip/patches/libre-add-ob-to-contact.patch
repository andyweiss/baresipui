--- a/src/sip/contact.c	2025-11-29 19:11:10.746624156 +0100
+++ b/src/sip/contact.c	2025-11-29 19:15:18.140290458 +0100
@@ -50,7 +50,8 @@
 	if (contact->uri && strchr(contact->uri, ':'))
 		return re_hprintf(pf, "Contact: <%s>\r\n", contact->uri);
 	else
-		return re_hprintf(pf, "Contact: <sip:%s@%J%s>\r\n",
+		/* Add ;ob parameter for RFC 5626 outbound support (NAT traversal) */
+		return re_hprintf(pf, "Contact: <sip:%s@%J;ob%s>\r\n",
 				  contact->uri,
 				  contact->addr,
 				  sip_transp_param(contact->tp));
