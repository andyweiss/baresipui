diff --git a/src/call.c b/src/call.c
index e7c9f63..c6621c4 100644
--- a/src/call.c
+++ b/src/call.c
@@ -2539,6 +2539,8 @@ static int sipsess_desc_handler(struct mbuf **descp, const struct sa *src,
 	MAGIC_CHECK(call);
 	call->af     = sa_af(src);
 	if (!call->acc->mnat)
+		/* PATCHED: Keep existing laddr (public IP) instead of overwriting with local src */
+		if (!sa_isset(sdp_session_laddr(call->sdp), SA_ADDR))
 		sdp_session_set_laddr(call->sdp, src);
 
 	if (list_isempty(&call->streaml)) {
